{
  "$schema": "https://opencode.ai/config.json",
  "skills": { "paths": ["~/.agents/skills"] },
  "command": {
    "agentcrew": {
      "description": "Load AgentCrew skill, route request to objective, assign squad",
      "template": "Load agentcrew skill. Read .agentcrew/00-objectives.md → match request → read objective → read 00-team.md → load role contracts → execute → log → update state."
    }
  }
}
